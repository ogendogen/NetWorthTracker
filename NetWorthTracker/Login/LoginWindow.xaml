<Window x:Class="NetWorthTracker.Login.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NetWorthTracker.Login"
        mc:Ignorable="d"
        Title="Net Worth Tracker" Height="175" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <Label Grid.Row="0" Content="Wybierz konto:" Margin="10,10,10,5"/>
        <ComboBox Grid.Row="1" ItemsSource="{Binding Path=Users}" 
                  SelectedItem="{Binding SelectedUser}"
                  DisplayMemberPath="Name"
                  Margin="10,5,10,10"/>
        
        <UniformGrid Grid.Row="2" Columns="2" Margin="10,10,10,10">
            <Button Content="Zaloguj" Command="{Binding Login}" Margin="0,0,5,0" Height="26"/>
            <Button Content="Stwórz" Command="{Binding OpenUserCreationWindow}" Margin="5,0,0,0" Height="26"/>
        </UniformGrid>
    </Grid>
</Window>
